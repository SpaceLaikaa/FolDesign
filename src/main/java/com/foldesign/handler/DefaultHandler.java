package com.foldesign.handler;

import java.nio.file.Path;

public class DefaultHandler extends FileHandler{
    public DefaultHandler(Path folderPath) {
        super(folderPath);
    }

    @Override
    public void handle(Path pathFile){

    }
}
